{
  "name": "notif-crypto-casino-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "collect": "node scripts/collect.js",
    "collect:summary": "node scripts/collect.js --mode=summary",
    "scheduled": "node scripts/run-scheduled.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "cheerio": "1.1.2",
    "playwright": "1.55.0"
  },
  "engines": {
    "node": ">=20"
  }
}
